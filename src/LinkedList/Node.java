package LinkedList;

public class Node {
    // below are the parameters of the variable
    int data;
    Node next;

    // below is a constructor in java
    // it allows for you to put in an argument when you make an instance of the class.
    Node(int input) {
       this.data = input;
       this.next = null;
    }
}
